{
  "name": "@1fi/shared",
  "version": "1.0.0",
  "description": "Shared types and pure-TypeScript business logic for the 1Fi Marketplace. No runtime dependencies — no React, no Express, no DOM APIs — so a future React Native app can import it unchanged.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^7.0.2",
    "vitest": "^3.2.4"
  }
}
